import TicketsList from "@/components/clientComponents/TicketsList/TicketsList";

export default function Admin() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      <TicketsList/>
    </main>
  );
}
